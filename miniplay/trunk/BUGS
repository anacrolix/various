* several dialogs need to be modal, but there is no top level window to set as parent
* the delete button is dangerously close to the next/previous buttons?
* an explicity play/pause toggle is needed in the context window
* non-audio media types are not entirely quashed, and image windows popup and
	cause failures in halting the pipeline
* volume control needed
* tags for songs in a libnotify style popup, or in the tooltips
* need to add some screenshots
* select music folder initialization doesn't select the default folder properly
* better icon
* compile instructions for several distros
* stream select option (editdialog style)
* correct previous when shuffle is on
